# AGENTS.md

本文件说明如何协作开发和维护 **ActionAnything**。这里的
“Agentic Harness Engineering（AHE）”是本仓库的维护方法：用可验证的
小迭代改进 ActionAnything；它**不是**要求把产品改造成新的模型
Harness、模型网关或自动化发布系统。

若本文件与更局部的说明冲突，优先遵循更局部的安全与兼容性约束；涉及
发布、凭据、权限或真实外部执行的决定交由维护者确认。

## 产品边界

ActionAnything 是应用程序与执行器之间的、策略门控的动作层。应用程序
拥有模型调用、提示词、API 密钥、会话、浏览器配置、确认界面和部署
隔离；ActionAnything 负责接收已规范化的动作、评估策略、请求确认、
调度执行器并记录本地证据。

- Adapter 是解析和规范化边界：它将受支持的提供方输出转换为
  `Action`，而不是替应用程序调用模型、保存密钥、回传屏幕内容或自行
  通过安全检查。
- 策略、域名允许列表和确认流程是防御层，不是对模型意图、网页语义、
  浏览器或网络隔离的证明。
- 不要在未经维护者同意的情况下添加提供方凭据、隐式网络调用、遥测、
  自动发布、宽泛的文件/桌面权限或绕过确认的默认行为。

## AHE 维护闭环

对行为、安全边界、公开 API 或执行器进行实质性改动时，按以下轻量闭环
工作：

1. **观察**：定位受影响组件、用户旅程和当前证据；先查看工作区状态，
   避免覆盖其他人的修改。
2. **假设**：写出可证伪的预期、范围外内容和风险边界。例如：
   “拒绝未支持的提供方字段可避免静默扩大动作能力”。
3. **小范围变更**：只做一个可独立审查、测试和回退的意图；不要把
   维护流程的名称误当成需要新增的产品功能。
4. **验证**：优先添加或更新离线、确定性的测试；运行与改动相匹配的
   检查，并区分“已运行的结果”与“计划运行的检查”。
5. **记录**：对实质性迭代，在 `docs/maintainer-iterations/` 或 PR 描述
   中留下精炼证据：观察、假设、变更、实际验证、残余风险和下一轮问题。
6. **下一轮**：如果证据不支持假设，缩小、修正或回退改动；不要用文档
   掩盖失败，也不要把未运行的检查写成通过。

拼写或纯格式调整可以不单独写迭代记录，但仍应检查链接、格式和相关
文档命令。

## 组件地图与改动边界

| 组件 | 主要位置 | 维护重点 |
| --- | --- | --- |
| 公共契约与动作输入 | `src/actionanything/actions.py`、`README.md` | 维持严格、可解释的规范化；扩展动作前明确最小风险与参数边界。 |
| 提供方 Adapter | `src/actionanything/adapters/` | 仅规范化受支持的载荷并保留最小来源信息；应用程序拥有模型调用和安全流程。 |
| 策略与确认 | `src/actionanything/policy.py`、`src/actionanything/runtime.py` | 默认保守；声明哪些风险不能仅靠语义或启发式判断。 |
| 执行器与浏览器边界 | `src/actionanything/executors.py` | 真实执行必须显式启用；允许列表不能替代隔离浏览器、网络或操作系统控制。 |
| 证据与回放 | `src/actionanything/recorder.py`、`src/actionanything/cli.py` | 不把 trace 当成保密系统或防篡改审计日志；避免在样例、日志和问题中暴露数据。 |
| 维护证据 | `docs/maintainer-iterations/` | 只保存精炼、可分享的工程证据；不得提交令牌、私密网页内容、个人数据或原始对话。 |

## 验证与人工边界

- 对代码改动，运行受影响的离线测试和 `git diff --check`；新文件在选择性
  暂存后，再运行 `git diff --cached --check`。
- 对 Adapter、策略、执行器和 trace 改动，覆盖拒绝路径、异常路径和成功
  路径，而不只覆盖“允许”的流程。
- 真实浏览器、外部服务和远程 CI 的结果不得凭空声称；在 PR 中写明本地
  实际验证、未运行部分及其人工验证计划。
- 改变默认确认、扩大真实执行权限、引入提供方网络调用/凭据、发布包、
  修改许可证或配置仓库权限前，必须获得维护者明确确认。
